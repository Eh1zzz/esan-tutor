# Phase 0 — data extraction + prep (runs locally; Python 3.10+)
pdfplumber>=0.11      # text + table extraction from the textbook PDF
PyMuPDF>=1.24         # fallback/alt PDF extractor (import as: fitz)
pandas>=2.0           # tabular data (vocab.csv etc.)

# Phase 1 — the from-scratch tiny GPT (src/esan/tinygpt.py). Colab already has
# torch; install locally only if you want to run/experiment off-Colab (CPU is fine
# for this tiny model, just slower):
#   torch>=2.2
# Phase 2 v2 — conversational tutor (src/esan/tutor_llm.py): the official Anthropic SDK.
# Set ANTHROPIC_API_KEY in your environment before running it.
anthropic>=0.40
# Phase 2 v3 — LoRA fine-tuning (src/esan/finetune_lora.py). Runs in Colab (has a
# GPU); these get pip-installed there. Base model google/mt5-small needs
# sentencepiece + protobuf for its tokenizer.
#   transformers>=4.46   peft>=0.11   datasets>=2.19   accelerate   sentencepiece   protobuf
